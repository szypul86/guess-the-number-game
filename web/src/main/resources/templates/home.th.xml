<thlogic>
    <attr sel="#headId"
          th:replace="~{fragments/head :: headFragment}" />
    <attr sel="#game-title" th:text="#{game.title}"/>
    <attr sel="#playLink"
          th:href="@{__${T(academy.learnprogramming.util.GameMappings).PLAY}__}"
          th:text="#{game.play}" />
       <attr sel="#footerId"
          th:replace="~{fragments/footer :: footerFragment}"/>


</thlogic>